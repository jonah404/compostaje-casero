rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App personal sin login: acceso abierto a las colecciones del tracker.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
